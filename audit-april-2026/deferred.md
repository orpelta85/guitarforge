# Deferred / Blocked Tasks

This file logs tasks that could not be completed during autonomous execution, with reason and recommended next action.

Started: 2026-04-28

## Format
| Phase | Task | Why Blocked | Recommended Next Action |
|-------|------|-------------|--------------------------|

## Blockers

| Phase | Task | Why Blocked | Recommended Next Action |
|-------|------|-------------|--------------------------|
| Phase 2 / Task 3b | Manual download required: cabinet IRs | All free IR sources (Redwirez, Kalthallen, Overdriven.fr, TONE3000) gate downloads behind email signup or browser flow — no direct curl/wget URLs available as of 2026-04-28. Code path (Task 3a) is complete and falls back to synth IRs gracefully. | User downloads 4 WAVs manually from Redwirez or Kalthallen, drops them into `/public/audio/cabs/` per the README. The Tab Player will pick them up automatically on next page load. |
| Phase 2 / Task 4b (partial) | Optional: clap.wav | Drumdrops free pack requires login; the GSCW open-source drum kit used for the other 7 samples doesn't include a clap. The Studio falls back to synth for clap (synthDrumHit case 4). | Optional - drop a `clap.wav` into `/public/audio/drums/` if you want a real-sample clap; not required. 7 of 8 instruments now use real samples (gregharvey/drum-samples GSCW kit, ~5.5 MB total). |
| Phase 3c / Task 5b | Region-aware playback (per-region Player.start scheduling) | The clip editor (move / trim / split / delete) is wired into the data model, project save/load, peaks rendering, and visual playback overlays. However, audio playback still uses one Tone.Player per track with the original full buffer — splits and trims are currently visual-only at playback time. Implementing per-region scheduling (multiple Player.start(when, offset, duration) calls per track + a regions->schedule recompute on edit, plus stop/restart on seek) is a deeper rewrite of the playAll/stopAll path that risks breaking the existing recording / YouTube / drum-sync flows. | Follow up: introduce a `RegionScheduler` that owns the per-track schedule and replace direct `nodes.player.start(...)` calls in `playAll`/`rewindToStart`/looping with `scheduler.start(trackId, transportOffset)`. Acceptance: trimming a recording's left edge by 2s makes playback skip the first 2s; deleting the second of two split halves removes its audio; export honours regions too. |
| Phase 7 / Part 2 | Split StudioPage.tsx into 15 sub-components | Original audit recommendation was a 1-2 week refactor: split the 3404-line StudioPage into TransportBar, MasterControls, TrackList, DrumPanel, SunoPanel, ExportMenu, ProjectHeader, etc.  Step 7 (this session) completed the foundation — Zustand store with 13 persistent state slices — but did NOT split the component, to keep the refactor safe within one session.  All sub-components could now read directly from `useStudioStore` instead of receiving props from StudioPage. | Follow up: extract one panel at a time (e.g. start with `<TransportBar />` reading `useStudioStore(s => s.bpm)`, `s.playing`, etc.), verify after each extraction with the runtime checklist (load project, BPM, solo/mute, save/load, export). Estimate: 4-6 sessions. |
| Phase 7 / Part 3 | Extract `useMediaRecorder` hook | StudioPage still owns ~150 lines of MediaRecorder + analyser ref management for the input meter and recording.  This is hook-shaped (mediaStream, mediaRecorder, recChunks, analyser, levelAnim) but tangled with `setIsRec`, `setRecTime`, `setRecLevel` local state. | Follow up: lift to `src/hooks/useMediaRecorder.ts` returning `{ isRec, recTime, recLevel, start, stop, level }`.  Independent of Zustand.  Estimate: 1 session. |
| Phase 7 / Part 4 | Refactor JamModePage and other pages to Zustand | Step 7 only touched StudioPage.  JamModePage, AiCoachPage, etc. still use local useState exclusively.  Not currently a problem — these pages are smaller (<1000 lines each) and not yet performance-bound. | Follow up: defer until/unless those pages start sharing state (e.g. cross-page settings, transport sync between Jam and Studio).  Add `useJamStore`, `useTransportStore` only when needed. |
